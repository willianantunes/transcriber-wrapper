[tox]
skipsdist=True
envlist =
    {py39}-standard

[testenv]
passenv = *
deps = pipenv
commands =
    pipenv install --dev --ignore-pipfile
    pytest tests -vv --cov=transcriber_wrapper --doctest-modules --junitxml=junit/test-results.xml --cov-report=xml --cov-report=html --cov-report=term
